<UserControl x:Class="QQLike.Components.EntryHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:components="clr-namespace:QQLike.Components"
             xmlns:converters="clr-namespace:QQLike.Converters"
             mc:Ignorable="d"
             Height="36"
             HorizontalAlignment="Stretch">
    <UserControl.Resources>
        <converters:BoolVisibilityConverter x:Key="BoolVisibilityConverter" />
    </UserControl.Resources>
    <Border Background="#fff" BorderBrush="#fff" BorderThickness="0,0,0,1"
            MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                <Image Width="30"
                       Height="30"
                       Margin="0,0,8,0"
                       Source="/Resource/images/favicon.ico" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button  Style="{StaticResource MaterialDesignFlatButton}" Width="45" Height="36"
                         VerticalAlignment="Center"
                         Visibility="{Binding MinimizeButtonVisible, RelativeSource={RelativeSource AncestorType={x:Type components:EntryHeader}}, Converter={StaticResource BoolVisibilityConverter}}"
                         Command="{Binding MinimizeCommand}">
                    <materialDesign:PackIcon Kind="Minus" Width="16" Height="16" />
                </Button>
                <Button VerticalAlignment="Center"
                        Width="45"
                        Height="36"
                        Command="{Binding ExitCommand}"
                        Style="{StaticResource MaterialDesignFlatButton}">
                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16" />
                </Button>
            </StackPanel>
            
        </Grid>
    </Border>
</UserControl>

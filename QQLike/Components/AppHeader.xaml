<UserControl x:Class="QQLike.Components.AppHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             Height="45" HorizontalAlignment="Stretch" VerticalAlignment="Top">
    <Border Background="{DynamicResource MaterialDesignPaper}"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="0,0,0,1"
            MouseLeftButtonDown="Header_MouseLeftButtonDown">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left: App Icon + User Info -->
            <StackPanel Grid.Column="0" Orientation="Horizontal"
                        VerticalAlignment="Center" Margin="10,0,0,0">
                <Image Width="30" Height="30" Margin="0,0,6,0"
                       Source="/Resource/images/favicon.ico" />

                <Button Width="40" Height="40" Margin="0,0,6,0"
                        Command="{Binding OpenUserProfileCommand}"
                        Style="{x:Null}"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Ellipse Width="36" Height="36"
                             Stroke="{DynamicResource MaterialDesignDivider}"
                             StrokeThickness="1">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{Binding UserAvatar}" Stretch="UniformToFill" />
                        </Ellipse.Fill>
                    </Ellipse>
                </Button>

                <TextBlock Text="{Binding Nickname}"
                           VerticalAlignment="Center"
                           FontSize="13" FontWeight="SemiBold"
                           Margin="0,0,8,0" />

                <TextBlock Text="{Binding Signature}"
                           VerticalAlignment="Center"
                           FontSize="12"
                           Foreground="{DynamicResource MaterialDesignHintForeground}"
                           MaxWidth="200"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>

            <!-- Right: Window Control Buttons -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Width="45" Height="36"
                        Command="{Binding MinimizeCommand}"
                        ToolTip="最小化">
                    <materialDesign:PackIcon Kind="Minus" Width="16" Height="16" />
                </Button>
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Width="45" Height="36"
                        Command="{Binding MaximizeCommand}"
                        ToolTip="最大化">
                    <materialDesign:PackIcon Kind="WindowMaximize" Width="16" Height="16" x:Name="MaximizeIcon" />
                </Button>
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Width="45" Height="36"
                        Command="{Binding ExitCommand}"
                        ToolTip="关闭">
                    <materialDesign:PackIcon Kind="Close" Width="16" Height="16" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
<Window x:Class="QQLike.Components.LoadingComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Height="250" 
             Width="400" 
             WindowStartupLocation="CenterScreen"
             WindowStyle="None">
    <Grid>
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Loading}" Value="True">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>

        <Border 
                Width="300"
                Height="300"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Grid Width="64" Height="64" HorizontalAlignment="Center">
                    <Ellipse Stroke="#335BC0FF" StrokeThickness="6" />
                    <Ellipse Stroke="#FF5BC0FF"
                             StrokeThickness="6"
                             StrokeDashArray="1.2 2.6"
                             StrokeStartLineCap="Round"
                             StrokeEndLineCap="Round"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotateTransform" />
                        </Ellipse.RenderTransform>
                    </Ellipse>
                </Grid>

                <TextBlock Text="{Binding LoadingText}"
                           Margin="0,16,0,0"
                           FontSize="15"
                           TextAlignment="Center"
                           HorizontalAlignment="Center" />

                <Button Content="取消"
                        Width="90"
                        Margin="0,18,0,0"
                        HorizontalAlignment="Center"
                        Command="{Binding CancelLoadingCommand}" />
            </StackPanel>
        </Border>

        <Grid.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <DoubleAnimation Storyboard.TargetName="SpinnerRotateTransform"
                                         Storyboard.TargetProperty="Angle"
                                         From="0"
                                         To="360"
                                         Duration="0:0:1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>
    </Grid>
</Window>
